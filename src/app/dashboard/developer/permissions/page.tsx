// This page has been moved to /dashboard/settings/permissions.
// This file is kept to prevent 404 errors from old links, but will be removed in the future.
import { redirect } from 'next/navigation';

export default function DeprecatedPermissionsPage() {
  redirect('/dashboard/settings/permissions');
}
